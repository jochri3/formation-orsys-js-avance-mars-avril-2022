const os = require("os");

// cpus : nombre de coeurs du CPU
console.log(os.cpus());

// freemem
console.log(os.freemem());

// totalmem
console.log(os.totalmem());

// hostmane
console.log(os.hostname());
